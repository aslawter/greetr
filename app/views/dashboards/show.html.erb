<h3>Hey <%=current_user.email %>! See all your yo's here</h3>
<%= form_for @greeting do |form| %>
  <div>
   <%= form.label :message, as: "Your message"%>
   <%= form.text_area :message, placeholder: "Say something" %>
  </div>
  <div>
    <%= form.submit "submit" %> 
  </div>
<% end %>  
